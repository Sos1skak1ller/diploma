# Система анализа спутниковых снимков


### 1. Требования
- Python 3.7 или выше
- Интернет для установки зависимостей

### 2. Установка (3 команды)

```bash
# 1. Клонируйте репозиторий
git clone <ваш-репозиторий>
cd programm

# 2. Создайте виртуальное окружение
python3 -m venv venv

# 3. Активируйте и установите зависимости
source venv/bin/activate  # Linux/Mac
# или
venv\Scripts\activate     # Windows

pip install -r requirements.txt
```

### 3. Запуск

```bash
# Демонстрация (рекомендуется для первого запуска)
python demo_interface.py

# Основное приложение
python launch_integrated.py

# Тесты
python test_interface.py
```

## 📋 Что входит в программу

### Три алгоритма:
1. **Детекция объектов** - Поиск кораблей, гаваней, теннисных кортов
2. **Зоны интересов** - Анализ областей на спутниковых снимках  
3. **Улучшение SAR снимков** - Специализированная обработка радиолокационных данных

### Возможности:
- Drag & Drop загрузка файлов
- Масштабирование и панорамирование изображений
- Настройка параметров алгоритмов
- Сохранение результатов

### SAR улучшение включает:
- **Автоматическое улучшение SAR** - адаптивный выбор параметров
- **SAR подавление шума** - специализированное удаление зернистости
- **SAR осветление** - улучшение темных областей
- **SAR контрастность** - CLAHE для радиолокационных данных
- **SAR резкость** - специальные ядра для границ объектов
- **Комплексное SAR улучшение** - полный пайплайн обработки
- Современный темный интерфейс

## 🔧 Альтернативные способы запуска

### Через Makefile (если установлен make):
```bash
make demo    # Демонстрация
make run     # Основное приложение
make test    # Тесты
```

### Через скрипт сборки:
```bash
chmod +x build.sh
./build.sh
```

## 🐛 Решение проблем

### Ошибка "Python не найден"
```bash
# Ubuntu/Debian
sudo apt install python3 python3-pip python3-venv

# macOS
brew install python3

# Windows - скачайте с python.org
```

### Ошибка "PyQt5 не найден"
```bash
pip install PyQt5
```

### Ошибка импорта модулей
- Убедитесь, что активировано виртуальное окружение
- Проверьте, что все файлы на месте

## 📁 Структура проекта

```
programm/
├── main_interface.py      # Главный интерфейс
├── new_controller.py      # Логика управления
├── demo_interface.py      # Демонстрация
├── view.py               # Компоненты отображения
├── model.py              # Модели алгоритмов
├── requirements.txt      # Зависимости
├── setup.py             # Установка пакета
├── Makefile             # Команды сборки
└── build.sh             # Скрипт сборки
```



## 🔬 Алгоритм улучшения качества снимков

### Базовый пайплайн (Baseline):
1. **Подавление шума:** Non-local Means Denoising для удаления зернистости
2. **Увеличение резкости:** Ядерная фильтрация для улучшения четкости

### Специальные методы для радиолокационных снимков (SAR):
- **SAR улучшение:** Полный пайплайн для темных SAR изображений
  - Логарифмическое растяжение
  - Специальное подавление шума
  - Улучшение контраста для темных областей
- **SAR подавление шума:** Адаптивное удаление зернистости SAR
- **SAR осветление:** Агрессивное осветление темных областей

### Дополнительные методы:
- **Подавление шума:** Настраиваемое удаление зернистости
- **Увеличение резкости:** Улучшение четкости изображения
- **Коррекция контраста:** CLAHE для улучшения контраста

### Метрики качества:
- **PSNR:** Peak Signal-to-Noise Ratio
- **Улучшение контраста:** Процентное улучшение контраста
- **Изменение яркости:** Процентное изменение яркости

### Тестирование алгоритма:
```bash
python test_enhancement.py
```

После выполнения команд выше программа готова к работе. Начните с демонстрации для знакомства с интерфейсом.

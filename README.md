# Satellite Image Analyzer

Система анализа спутниковых радиолокационных (SAR) снимков с графическим интерфейсом и алгоритмами детекции/улучшения изображений.

## 📋 Описание проекта

Проект представляет собой комплексную систему для обработки и анализа спутниковых радиолокационных снимков, включающую:

- **Улучшение качества SAR изображений** - специализированные алгоритмы шумоподавления, осветления и повышения контраста для темных радиолокационных снимков
- **Детекция объектов** - автоматическое обнаружение объектов на снимках с использованием YOLO и GATE алгоритмов
- **Графический интерфейс** - удобный GUI на PyQt5 для работы с изображениями
- **AI-модели** - поддержка нейросетевых моделей для улучшения качества изображений

### Основные возможности

- ✅ Множество алгоритмов улучшения SAR изображений (baseline, denoise, sar_auto_enhance, sar_comprehensive и др.)
- ✅ Адаптивные параметры обработки в зависимости от характеристик изображения
- ✅ Обработка изображений любого размера с использованием tiling
- ✅ Метрики качества улучшения (PSNR, контраст, яркость)
- ✅ Интеграция с нейронными сетями для AI-обработки
- ✅ Детекция объектов на радиолокационных снимках

## 🚀 Быстрый старт

### Требования

- Python >= 3.9
- PDM (Python Dependency Manager) или pip

### Установка PDM

PDM - это современный менеджер зависимостей для Python. Установите его одним из способов:

**Через pip:**
```bash
pip install pdm
```

**Через Homebrew (macOS):**
```bash
brew install pdm
```

**Через curl (Linux/macOS):**
```bash
curl -sSL https://pdm-project.org/install-pdm.py | python3 -
```

Проверьте установку:
```bash
pdm --version
```

### Установка проекта

#### Вариант 1: Использование PDM (рекомендуется)

1. **Клонируйте репозиторий** (если необходимо):
```bash
git clone <repository-url>
cd diplom
```

2. **Установите зависимости через PDM:**
```bash
pdm install
```

PDM автоматически создаст виртуальное окружение и установит все необходимые зависимости из `pyproject.toml`.

3. **Активируйте окружение PDM:**
```bash
eval $(pdm venv activate)
```

Или используйте команды через PDM:
```bash
pdm run python -m main.interface.run_interface
```

#### Вариант 2: Использование venv и pip

1. **Создайте виртуальное окружение:**
```bash
make venv
source .venv/bin/activate
```

2. **Установите зависимости:**
```bash
make install
```

Или напрямую через pip:
```bash
pip install -r programm/requirements.txt
```

### Установка дополнительных зависимостей

**Для разработки:**
```bash
pdm install -G dev
```

**Для поддержки ONNX моделей:**
```bash
pdm install -G onnx
```

## 📦 Запуск приложения

### Через Makefile (рекомендуется)

```bash
# Запустить основное GUI приложение
make run

# Запустить демо-интерфейс
make demo

# Запустить тесты
make test

# Очистить временные файлы
make clean
```

### Напрямую через Python

**Основной интерфейс:**
```bash
python -m main.interface.run_interface
```

**Демо-интерфейс:**
```bash
python -m main.interface.demo_interface
```

**Через PDM:**
```bash
pdm run python -m main.interface.run_interface
```

## 📁 Структура проекта

```
diplom/
├── main/                           # Основной пакет проекта
│   ├── algorythms/                 # Алгоритмы обработки
│   │   ├── area_selection/         # Детекция объектов (YOLO, GATE)
│   │   │   └── sar_gate_yolo/      # SAR детекция
│   │   ├── improvment/             # Улучшение изображений
│   │   │   ├── denoising_models.py # Модели шумоподавления
│   │   │   └── image_enhancement.py # Алгоритмы улучшения SAR
│   │   └── classification/         # Классификация (в разработке)
│   ├── interface/                  # Графический интерфейс
│   │   ├── run_interface.py        # Основное приложение
│   │   ├── demo_interface.py       # Демо-интерфейс
│   │   ├── main_interface.py       # GUI компоненты
│   │   └── enhanced_components.py  # Расширенные компоненты
│   └── tests/                      # Тесты
│       ├── test_interface.py
│       ├── test_enhancement.py
│       └── test_sar_enhancement.py
├── images/                         # Примеры изображений
│   └── enhanced/                   # Обработанные изображения
├── legacy/                         # Старая версия (для справки)
├── research_algorithm/             # Исследовательские алгоритмы
├── pyproject.toml                  # Конфигурация PDM
├── pdm.lock                        # Lock-файл зависимостей
├── Makefile                        # Команды для сборки/запуска
└── README.md                       # Этот файл
```

## 🛠️ Основные команды PDM

```bash
# Установка зависимостей
pdm install

# Добавление новой зависимости
pdm add <package-name>

# Добавление dev-зависимости
pdm add -d <package-name>

# Обновление зависимостей
pdm update

# Запуск скриптов
pdm run python <script.py>

# Просмотр установленных пакетов
pdm list

# Создание lock-файла
pdm lock

# Удаление виртуального окружения
pdm venv remove

# Показать путь к активному окружению
pdm venv list
```

## 🔧 Алгоритмы улучшения SAR изображений

Проект включает следующие методы обработки:

- **baseline** - базовое улучшение (денойзинг + резкость)
- **denoise** - подавление шума
- **sharpen** - увеличение резкости
- **contrast** - улучшение контраста
- **sar_enhancement** - специализированное улучшение для SAR
- **sar_denoise** - SAR-специфичное шумоподавление
- **sar_brighten** - осветление темных SAR снимков
- **sar_auto_enhance** - автоматическое улучшение с адаптивными параметрами
- **sar_contrast** - улучшение контраста для SAR
- **sar_sharpen** - увеличение резкости для SAR
- **sar_comprehensive** - комплексная обработка SAR
- **sar_ai_denoise** - AI-шумоподавление
- **sar_ai_enhance** - AI-улучшение
- **sar_morphological_enhance** - морфологическое улучшение

## 🧪 Тестирование

Запустить все тесты:
```bash
make test
```

Или отдельные тесты:
```bash
python -m main.tests.test_enhancement
python -m main.tests.test_interface
python -m main.tests.test_sar_enhancement
```

## 📊 Примеры использования

### Программный интерфейс

```python
from main.algorythms.improvment.image_enhancement import ImageEnhancement

# Создаем объект для улучшения изображений
enhancer = ImageEnhancement()

# Улучшаем изображение
enhanced_img, metrics = enhancer.enhance_image(
    image_path="images/1.jpg",
    method="sar_auto_enhance",
    intensity=70
)

# Сохраняем результат
enhancer.save_enhanced_image(enhanced_img, "images/enhanced/output.jpg")

# Выводим метрики
print(f"PSNR: {metrics['psnr']}")
print(f"Улучшение контраста: {metrics['contrast_improvement']}%")
print(f"Изменение яркости: {metrics['brightness_change']}%")
```

## 🤝 Разработка

### Добавление новых зависимостей

```bash
# Основная зависимость
pdm add numpy

# Dev-зависимость
pdm add -d pytest

# Опциональная зависимость (например, для ONNX)
pdm add -G onnx onnxruntime
```

### Обновление зависимостей

```bash
pdm update
```

### Очистка проекта

```bash
make clean
```

## 📝 Лицензия

Дипломный проект

## 👥 Авторы

Diplom Project Team

## 🐛 Известные проблемы и решения

**Проблема:** PDM не найден после установки
```bash
# Решение: Добавьте PDM в PATH или используйте полный путь
export PATH="$HOME/.local/bin:$PATH"
```

**Проблема:** Ошибки импорта модулей
```bash
# Решение: Убедитесь, что вы в корневой директории проекта
cd /path/to/diplom
pdm run python -m main.interface.run_interface
```

**Проблема:** Недостаточно памяти для больших изображений
```bash
# Решение: Система автоматически использует tiling для больших изображений
# Можно настроить параметры в ImageEnhancement:
# self.max_tile_size = 4096
# self.overlap_size = 128
```

## 📚 Дополнительная информация

- [Документация PDM](https://pdm-project.org/)
- [PyQt5 Documentation](https://www.riverbankcomputing.com/static/Docs/PyQt5/)
- [OpenCV Documentation](https://docs.opencv.org/)

---

**Версия:** 2.0.0  
**Последнее обновление:** 2025

